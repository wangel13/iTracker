<%= form_for(team_user) do |f| %>
  <% if team_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_user.errors.count, "error") %> prohibited this team_user from being saved:</h2>

      <ul>
      <% team_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :team_id, 'Team' %>
    <%= f.collection_select :team_id, Team.all, :id, :description,
      { :include_blank => true},
      {:class => 'form-control'} %>
  </div>

  <div class="form-group">
    <%= f.label :team_id, 'User' %>
    <%= f.collection_select :user_id, User.all, :id, :name,
      { :include_blank => true},
      {:class => 'form-control'} %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
