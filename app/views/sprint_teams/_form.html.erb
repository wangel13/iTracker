<%= form_for(sprint_team) do |f| %>
  <% if sprint_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sprint_team.errors.count, "error") %> prohibited this sprint_team from being saved:</h2>

      <ul>
      <% sprint_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sprint_id %>
    <%= f.text_field :sprint_id %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
